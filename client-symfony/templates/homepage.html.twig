{% extends 'base.html.twig' %}

{% block body %}
    <pre>{{ dump(contentNew) }}</pre>
    <pre>{{ dump(contentUpdate) }}</pre>
    <pre>{{ dump(contentshow) }}</pre>
    <pre>{{ dump(contents) }}</pre>
{% endblock %}
